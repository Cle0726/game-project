# 第一至三章合体分离记录（2026-07-05）

## 当前结构决定

- `chapter1_start` 已改为“新第一章重做占位入口”。
- 旧版第一章、第二章、第三章内容不删除，统一暂存到 `chapter3_archive_start`。
- 世界地图中新增/调整为：
  - `chapter1_rebuild`：新第一章区域，只保留 `chapter1_start` 一个入口，等待重做。
  - `chapter3_legacy_combined`：第三章旧案合辑，挂载旧 `ch1_*`、`ch2_*`、`ch3_*` 关键节点。

## 旧内容归档范围

- 旧第一段：`ch1_*`，共 18 个场景。
- 旧第二段：`ch2_*`，共 13 个场景。
- 旧第三段：`ch3_*`，共 8 个场景。
- 它们现在统一按“第三章旧案合辑”显示章节归属。

## 新第一章入口原则

- 新第一章不再从旧灰弦公路 / 回声城邦晨钟线开始。
- 新第一章应从第零章之后继续：阿缇娅、未鸣契约、白谱院追查、主角母亲线。
- 后续重做第一章时直接扩展 `chapter1_start` 或添加新的 `ch1_new_*` 场景，避免复用旧 `ch1_*`。

## 已验证

- `node --check .\game.js`：通过
- `npm run battle:guard`：通过
- `npm run portrait:build`：通过
- 场景 choice 跳转缺失：0
- 素材引用缺失：0
