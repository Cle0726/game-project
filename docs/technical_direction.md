# 技术方向

当前项目不再受早期 P0 原型的技术栈限制约束。

已取消的限制：

- 不再限制为纯 HTML + CSS + JavaScript。
- 不再限制文件只能有 `index.html`、`style.css`、`game.js` 三个。
- 不再限制不能使用框架、npm 包或第三方动画库。
- 不再限制只能使用 `localStorage` 作为唯一存档方案。

当前根目录原型仍保持静态页面可直接运行，作为已完成的轻量版本保留。后续可以在此基础上升级为 Vite、React、Vue、Svelte、PixiJS、GSAP、Three.js 或其他更适合的工程结构。
